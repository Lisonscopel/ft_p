#include "server.h"

int			lcd(char **path, int socket)
{
	(void)socket;
	(void)path;
	return (0);
}

int			lpwd(char **path, int socket)
{
	(void)socket;
	(void)path;
	return (0);
}

int			lls(char **path, int socket)
{
	(void)socket;
	(void)path;
	return (0);
}
